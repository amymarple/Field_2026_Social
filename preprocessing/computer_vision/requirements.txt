# Field-PC CV pipeline dependencies.
#
# IMPORTANT (Blackwell / RTX 5060 Ti, sm_120): install PyTorch FIRST from the
# cu128 index, otherwise the default wheel lacks sm_120 and the GPU op fails:
#
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
#
# Then install the rest with this file:
#   pip install -r requirements.txt

ultralytics>=8.3
opencv-python>=4.10
av>=12.0
numpy>=1.26
pandas>=2.2
matplotlib>=3.8
tqdm>=4.66
shapely>=2.0
